# Plugins Directory

This directory is reserved for external plugin JARs.

- Place plugin artifacts here (for example: `modules/plugins/nas-plugin.jar`).
- No source code is stored in this directory.
